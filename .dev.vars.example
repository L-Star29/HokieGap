# Copy to .dev.vars. Never commit the filled-in file.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
DATABRICKS_HOST=https://dbc-31de3f1b-fd1c.cloud.databricks.com
DATABRICKS_TOKEN=
DATABRICKS_WAREHOUSE_ID=
DATABRICKS_CATALOG=workspace
DATABRICKS_SCHEMA=hokiegap
# Enable only after verifying credentials and setting a sensible request budget.
AGENT_ENABLED=false
